<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryDark">@color/color_primary_dark</item>
        <item name="colorAccent">@color/color_accent</item>
    </style>

    <style name="ReviewEditTextBase" parent="TextAppearance.AppCompat.Body1">
        <item name="android:background">@drawable/background_review_text</item>
        <item name="android:padding">@dimen/spacing_normal</item>
    </style>

    <style name="BathroomNameEditTextBase" parent="ReviewEditTextBase">
        <item name="android:layout_margin">@dimen/padding_normal</item>
    </style>

</resources>
